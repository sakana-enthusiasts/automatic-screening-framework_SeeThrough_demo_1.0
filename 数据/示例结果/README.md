# 示例结果说明

为避免提交可由程序重建的大量中间 CSV、缓存与运行结果，本目录不重复保存完整候选记录。

- 代表性完整结果见 `../导出结果/SeeThrough规则筛选演示报告.xlsx`；
- 两张筛选结果图和一张规则筛选界面图见 `../导出结果/图表/`；
- 运行输入见 `../论文原始数据/`；`../示例数据/` 包含用于离线复核的最终 10 候选标签表，以及补充数据 2/3 的小型人工核对结构映射表。

执行本项目的流程后，新的中间结果、模型结果、筛选结果、软件缓存和用户导入文件均只保存在本地，并由 `.gitignore` 排除。
