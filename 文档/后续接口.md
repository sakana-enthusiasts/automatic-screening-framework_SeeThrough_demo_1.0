# 后续接口

## 高斯过程（GP）透明化预测

**状态：接口预留，当前演示未启用。**

`插件/透明化预测/统一预测接口.py` 和 `当前高斯过程预测插件.py` 为未来模型接入保留。启用前需要版本化的训练集、明确的特征定义、独立验证、误差与适用域报告；在满足这些条件前，界面不应把结果解读为透明化效果证明。

## BoTorch 实验优化

**状态：接口预留，当前演示未启用。**

`插件/实验优化/统一优化接口.py` 和 `BoTorch实验优化插件.py` 为未来实验迭代预留。启用前需要经过审核的实验历史、目标函数、约束条件、随机性处理和人工复核流程。

## 在线化学与毒性查询

**状态：部分实现。**

PubChem 与 CompTox 插件保留了查询接口。在线结果和缓存不随仓库发布；可选密钥只能在本地环境变量中配置。使用在线证据不替代按实验室标准进行的毒理学评价。
